import { ProductFilterDataPipe } from './product-filter-data.pipe';

describe('ProductFilterDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterDataPipe();
    expect(pipe).toBeTruthy();
  });
});
